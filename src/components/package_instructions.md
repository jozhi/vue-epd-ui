``` base
{
  ...
  "version": "1.0.16",  // 每次更改代码之后需要更改版本号，才能发布到npm。
  "private": false,  // 是否设置为私有包
  "main": "./epdUI/epdUI.min.js",   // 配置组件包入口地址
  "files": [  // 要上传到npm的文件夹
    "epdUI"
  ],
  ...
}

```